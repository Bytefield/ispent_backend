version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '443:3000'
    environment:
      NODE_ENV: production
      MONGO_USER: papi
      MONGO_PASS: OTkwLNke1sHFBcxp
    depends_on:
      - db
  db:
    image: mongo
    environment:
      MONGO_INITDB_ROOT_USERNAME: papi
      MONGO_INITDB_ROOT_PASSWORD: OTkwLNke1sHFBcxp
    ports:
      - '27017:27017'